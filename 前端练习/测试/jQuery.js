!function(){
    function $(ele){                    //获取DOM元素
        return typeof(ele) === 'string'?document.getElementById(ele):ele;
    }

    function getElementsByClassName(opts){    //封装兼容所有浏览器的getElementsByClassName
        
    }
}();